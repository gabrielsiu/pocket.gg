query AllTournaments($perPage: Int, $pageNum: Int) {
    tournaments(query: {
                perPage: $perPage
                page: $pageNum
                }) {
        nodes {
            name
            id
        }
    }
}
